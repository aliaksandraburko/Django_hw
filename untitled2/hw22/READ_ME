Получение всех альбомов группы

from hw22.models import MusicBand, Album, Track
MusicBand.objects.filter()

Получение всех треков альбома
Track.objects.filter(which_albums = 'AM')

Получение всех треков группы
Track.objects.filter(title='Arctick Monkeys')

Получение всех альбомов, группы которых были основаны в 1990 году
Album.objects.filter(year_of_issue='2007')
